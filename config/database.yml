# SQLite is used for local development and test. Production runs on a single
# PostgreSQL database (Supabase) configured via DATABASE_URL. The whole Solid
# stack (Queue/Cache/Cable) lives on the primary connection in every
# environment for full dev/prod parity — see db/migrate for their tables.
sqlite: &sqlite
  adapter: sqlite3
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  primary:
    <<: *sqlite
    database: storage/development.sqlite3

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *sqlite
  database: storage/test.sqlite3

# Production: a single Postgres database read from DATABASE_URL (the Supabase
# session-pooler connection string). prepared_statements: false keeps the app
# safe across Supabase's transaction/session pooler modes.
production:
  primary:
    adapter: postgresql
    url: <%= ENV["DATABASE_URL"] %>
    prepared_statements: false
    pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
